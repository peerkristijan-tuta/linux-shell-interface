Example inputs to command prompt from program:
- ls
- ls -l
- ls -l -a
- ls | grep shell

the command '|' allows piping

- !

the command '!' causes the previous entered command to be re-execute

- sleep 3 &
  
the command '&' causes the command to run concurrently 
